{
  "scripts": {
    "build": "cd redi-web && npm install && npm run build",
    "dev": "npm run dev --workspaces",
    "heroku-postbuild": "cd backend && npm run build"
  },
  "devDependencies": {
    "prettier": "^3.6.2"
  }
}
