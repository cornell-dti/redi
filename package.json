{
  "workspaces": [
    "frontend",
    "redi-web"
  ],
  "scripts": {
    "build": "npm install && npm run build --workspaces",
    "dev": "npm run dev --workspaces"
  },
  "devDependencies": {
    "prettier": "^3.6.2"
  }
}
